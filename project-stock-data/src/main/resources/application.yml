# src/main/resources/application.yml
server:
  port: 8091

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bootcamp_2508_final_project
    username: postgres
    password: admin1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  data:
    redis:
      host: localhost
      port: 6379

app:
  upload:
    dir: uploads
  provider:
    base-url: localhost:8090
    api-token: d1n3u8hr01qlvnp5dhv0d1n3u8hr01qlvnp5dhvg